
services:
  line-openclaw-proxy:
    build: .
    container_name: line-openclaw-proxy
    restart: unless-stopped
    ports:
      - "8283:8283"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    env_file:
      - .env
